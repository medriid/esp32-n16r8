#pragma once

// Copy this file to include/wifi_secrets.h and replace the example values.
// The real file is ignored by Git so your password is not committed.
#define WIFI_SSID "your-wifi-name"
#define WIFI_PASSWORD "your-wifi-password"
